Installe Node JS Firstly

Electron JS requirements
1. npm install electron
2. npm install electron-log
3. npm install python-shell

Python Install Requirements
>>Open CMD and write command:- pip install -r requirements.txt
The above command install all the reequirements for python

After installation all python requirements
>>run command on cmd:- pyinstaller --onefile loginsignup.py
After successful the above command go to Build directory and copy loginsignup.exe file and paste at root directory. 